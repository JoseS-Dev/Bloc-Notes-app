export interface UserData{
    name_user: string,
    last_name_user: string,
    username: string,
    email_user: string,
    password_user: string
}

export interface loginData{
    email_user: string,
    password_user: string
}